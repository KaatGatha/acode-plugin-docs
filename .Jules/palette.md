## 2024-05-20 - Keyboard shortcuts formatting
**Learning:** Found that keyboard shortcuts in the markdown documentation were using inline code blocks (e.g. `\`Ctrl\``) instead of `<kbd>` tags.
**Action:** Replace inline code blocks representing keys with `<kbd>` elements in markdown for better visual styling and semantic HTML.
