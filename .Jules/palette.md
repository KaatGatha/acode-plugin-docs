## 2026-09-13 - Semantic Keyboard Shortcuts
**Learning:** Using semantic `<kbd>` tags instead of inline markdown code blocks for keyboard shortcuts improves both visual presentation and screen reader accessibility.
**Action:** Use `<kbd>` tags for keyboard shortcuts in markdown files.
