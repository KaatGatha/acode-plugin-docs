## 2024-05-18 - Keyboard shortcut styles
**Learning:** The documentation uses markdown inline code blocks like `Ctrl` for keyboard keys, but using standard HTML `<kbd>` elements with proper CSS styling would significantly improve the visual hierarchy and UX for keyboard shortcut documentation.
**Action:** Add CSS for `kbd` elements to `.vitepress/theme/style.css`.
